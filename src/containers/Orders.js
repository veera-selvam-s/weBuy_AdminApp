import React from 'react';
import Layout from '../components/Layout'

const Orders = (props) => {
    return(
        <Layout sidebar>
            <div>Orders</div>
        </Layout>
        
    )
}
export default Orders;